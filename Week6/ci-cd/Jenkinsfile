pipeline {
    agent any

    parameters {
        choice(
            name: 'TARGET_ENV',
            choices: ['dev', 'stage', 'prod'],
            description: 'Select the deployment environment'
        )
        booleanParam(name: 'APPLY_INFRA',   defaultValue: false, description: '✅ Apply Terraform changes automatically')
        booleanParam(name: 'DESTROY_INFRA', defaultValue: false, description: '💣 Destroy infrastructure for selected environment')
        booleanParam(name: 'ROLLBACK_ON_FAILURE', defaultValue: true, description: '🔁 Rollback infrastructure if apply fails')
    }

    environment {
        ENV         = "${params.TARGET_ENV}"
        TF_DIR      = "${WORKSPACE}/Week6"
        TF_VAR_FILE = "envs/${ENV}/${ENV}.tfvars"
        AWS_REGION  = "us-east-1"
    }

    options {
        timestamps()
        disableConcurrentBuilds()
        buildDiscarder(logRotator(numToKeepStr: '10'))
    }

    stages {

        stage('Checkout SCM') {
            steps {
                echo "🔄 Checking out source code..."
                checkout scm
            }
        }

        stage('Parallel Checks') {
            when { expression { !params.DESTROY_INFRA } }
            parallel {
                stage('Terraform Fmt') {
                    steps {
                        dir("${TF_DIR}") {
                            sh "terraform fmt -check -recursive || true"
                        }
                    }
                }
                stage('Security Scan (tfsec)') {
                    steps {
                        dir("${TF_DIR}") {
                            sh "tfsec . || true"
                        }
                    }
                }
            }
        }

        stage('Terraform Init') {
            steps {
                dir("${TF_DIR}") {
                    echo "🚀 Initializing Terraform backend for ${ENV}"
                    sh """
                    terraform init -reconfigure -input=false \
                      -backend-config=envs/${ENV}/backend.conf \
                      -backend-config=key=terraform/${ENV}/terraform.tfstate \
                      -var-file=${TF_VAR_FILE}
                    """
                }
            }
        }

        stage('Terraform Validate') {
            when { expression { !params.DESTROY_INFRA } }
            steps {
                dir("${TF_DIR}") {
                    sh "terraform validate"
                }
            }
        }

        stage('Terraform Plan') {
            when { expression { !params.DESTROY_INFRA } }
            steps {
                dir("${TF_DIR}") {
                    echo "📋 Creating Terraform plan for ${ENV}..."
                    sh """
                    terraform plan -input=false \
                      -var-file=${TF_VAR_FILE} \
                      -out=tfplan
                    terraform show -no-color tfplan > tfplan.txt
                    """
                    archiveArtifacts artifacts: 'tfplan.txt', fingerprint: true
                }
            }
        }

        stage('Manual Approval (for PROD)') {
            when {
                allOf {
                    expression { params.TARGET_ENV == 'prod' }
                    expression { !params.DESTROY_INFRA }
                }
            }
            steps {
                input message: "⚠️ Approve deployment to *PROD* environment?", ok: "Deploy"
            }
        }

        stage('Terraform Apply') {
            when {
                allOf {
                    expression { params.APPLY_INFRA }
                    expression { !params.DESTROY_INFRA }
                }
            }
            steps {
                dir("${TF_DIR}") {
                    echo "⚡ Applying Terraform for ${ENV}..."
                    script {
                        def applyStatus = sh(script: "terraform apply -input=false tfplan", returnStatus: true)
                        if (applyStatus != 0) {
                            echo "❌ Terraform apply failed for ${ENV}"
                            if (params.ROLLBACK_ON_FAILURE) {
                                echo "🔁 Rolling back ${ENV} infrastructure..."
                                sh "terraform destroy -auto-approve -var-file=${TF_VAR_FILE}"
                                notifySlack("⚠️ Rollback triggered for *${ENV}* after failed apply")
                            }
                            error("Terraform apply failed with exit code ${applyStatus}")
                        }
                    }
                }
            }
        }

        stage('Terraform Destroy') {
            when { expression { params.DESTROY_INFRA } }
            steps {
                dir("${TF_DIR}") {
                    input message: "💣 Confirm destroy of *${ENV}* environment?", ok: "Yes, destroy it!"
                    echo "🔥 Destroying Terraform resources for ${ENV}..."
                    sh """
                    terraform destroy -auto-approve -var-file=${TF_VAR_FILE}
                    """
                }
            }
        }

        // Auto-Promotion: DEV → STAGE
        stage('Promote to Stage') {
            when {
                allOf {
                    expression { params.TARGET_ENV == 'dev' }
                    expression { currentBuild.currentResult == 'SUCCESS' }
                }
            }
            steps {
                echo "🚀 Dev deployment successful — promoting to Stage..."
                build job: env.JOB_NAME,
                      parameters: [
                          string(name: 'TARGET_ENV', value: 'stage'),
                          booleanParam(name: 'APPLY_INFRA', value: true)
                      ],
                      wait: false
            }
        }

        // Manual Promotion: STAGE → PROD
        stage('Promote to Prod') {
            when {
                allOf {
                    expression { params.TARGET_ENV == 'stage' }
                    expression { currentBuild.currentResult == 'SUCCESS' }
                }
            }
            steps {
                input message: "🚦 Approve promotion to *PROD*?", ok: "Proceed"
                build job: env.JOB_NAME,
                      parameters: [
                          string(name: 'TARGET_ENV', value: 'prod'),
                          booleanParam(name: 'APPLY_INFRA', value: true)
                      ],
                      wait: false
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline completed successfully for ${ENV}"
            script { notifySlack("✅ Terraform Apply succeeded for *${ENV}*") }
        }

        failure {
            echo "❌ Pipeline failed for ${ENV}"
            script { notifySlack("❌ Terraform Pipeline failed for *${ENV}*") }
        }

        always {
            echo "📦 Archiving artifacts and cleaning workspace..."
            archiveArtifacts artifacts: '**/tfplan.txt', onlyIfSuccessful: false
            cleanWs(deleteDirs: true)
        }
    }
}

/**
 * Slack Notification Helper
 */
def notifySlack(String message) {
    try {
        withCredentials([string(credentialsId: 'slack_webhook_url', variable: 'SLACK_WEBHOOK_URL')]) {
            sh """
            curl -X POST -H 'Content-type: application/json' \
            --data '{"text": "${message} - Job: ${env.JOB_NAME} #${env.BUILD_NUMBER}"}' \
            "\${SLACK_WEBHOOK_URL}"
            """
        }
    } catch (err) {
        echo "ℹ️ Slack webhook not configured; skipping notification."
    }
}
